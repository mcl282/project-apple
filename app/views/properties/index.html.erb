
<table class="table table-hover">
  <tr>
    <th>Property Name</th>
    <th>Property Address</th>
  </tr>
    <% @property.each do |property| %>    
  <tr>
    <td><p><%= link_to "#{property.property_name}", edit_property_path(property.id) %><%=  %></p></td>
    <td><p><%= property.property_address %></p></td>
  </tr>
    <% end %>
</table>
    
